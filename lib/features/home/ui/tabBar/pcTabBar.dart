import 'package:flutter/material.dart';

class PcTabBar extends StatelessWidget {
  const PcTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}